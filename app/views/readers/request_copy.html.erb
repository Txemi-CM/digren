<h1>Get your sample of Digital Renaissance</h1>

<div class="row">
  <div class="offset1 span3 center">
    <br><br>
    <%= image_tag("digital_renaissance_cover_50.jpg", :alt => "Digital Renaissance") %>
  </div>

  <div class="span6 offset1">
    <h2>Get it the traditional way...</h2>
    <p>Enter your email address and we will send you a pdf sample of the book.</p>
    
    <%= form_for(@reader) do |f| %>
      <% if @reader.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@reader.errors.count, "error") %> Please double check your entries:</h2>

          <ul>
          <% @reader.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :name %><br />
        <%= f.text_field :name %>
      </div>
      <div class="field">
        <%= f.label :email %><br />
        <%= f.text_field :email %>
      </div>
       
      <div class="actions">
        <%= f.submit "Send me a pdf sample", class: "btn btn-large btn-primary" %>
      </div>
    <% end %>
    <h2>Get it the Twitter way...</h2>
    <p>You can also tweet with a link to this page and if you follow us, we will send you a DM with the link to download the sample.</p>
    
    <%= render 'layouts/tweet' %>
  </div>
</div>